# VM ~/truewords/preserve-images.txt로 복사한다. 실제 전환 직전 태그를 한 줄씩 기록한다.
# deploy-*의 자동 GC와 주간 cron 모두 이 파일을 읽는다.
# truewords-admin:<전환전-통합앱-sha>
# truewords-backend:<전환전-api-sha>
# truewords-web:<직전-검증-sha>
