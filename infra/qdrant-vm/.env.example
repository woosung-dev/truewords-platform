# Qdrant API key (32바이트 랜덤 권장: openssl rand -base64 32)
QDRANT_API_KEY=replace-me

# Cloudflare Tunnel token
# Cloudflare Zero Trust → Networks → Tunnels → 터널 생성 후 발급
CLOUDFLARE_TUNNEL_TOKEN=replace-me
