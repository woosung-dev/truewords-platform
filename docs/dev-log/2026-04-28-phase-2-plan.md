# Phase 2 — Contextual Retrieval + Source Ablation Plan

> **상태:** 옵션 D (NotebookLM 200건 빠른 평가) 진행 중. 결과에 따라 B 단독 / B+G 병행 / 액션 1+2 롤백 검토로 분기.
> **인계서:** `~/Downloads/truewords_phase_2_handoff.md`
> **선행 PR:** #68 (`c2bb05b`) — IntentClassifierStage + RAGAS 평가 도입 (액션 1+2+3)
> **브랜치:** `feat/phase-2-contextual-retrieval-and-source-ablation` (main에서 분기)
> **세션 plan 파일:** `/Users/woosung/.claude/plans/downloads-truewords-phase-2-handoff-md-refactored-hickey.md`

---

## 0. 목표 / 근거

직전 세션 RAGAS 3-way 결과 (인계서 §1):

```
                  baseline   +action2    +action1+2
faithfulness        0.541     0.512       0.546
context_precision   0.630     0.578       0.626
context_recall      0.400     0.340       0.425  ★
answer_relevancy    0.764     0.784       0.779
```

**정정된 해석 (Codex 교정 후 — 인계서 §1)**:
- faithfulness +0.005 = noise 수준, 의사결정 근거 불가
- context_recall +0.025 = 유망 신호이나 n=36~40에서는 확정 승리 아님
- 결론: **"Action2 단독 금지 / Action1+2는 추가 검증 대상"**

Phase 2의 목적은 retrieval 자체의 품질을 끌어올리고 측정 신뢰도를 회복하는 것이다.

---

## 1. 옵션 우선순위 (인계서 §0)

| 우선 | 작업 | 공수 | 본 plan 다룸? |
|---|---|---|---|
| 🥇 D | NotebookLM 200건 휴리스틱 빠른 평가 | 1h | ✅ 본 세션 |
| 🥈 B | Anthropic Contextual Retrieval (615권 재인덱싱) | 5~7d | 후속 plan |
| 🥈 G | Source-weight ablation (per-source 메트릭 분해) | 0.5~1d | 후속 plan |
| 🥉 F | 청킹 자연 단위 재설계 PoC (1권 샘플) | 1~2d | 후속 plan |
| 🥉 H | Citation enforcement eval | 1~2d | 후속 plan |

**보류 (당분간 안 함)**: 옵션 C Admin UI toggle / 옵션 E Cross-encoder reranker / 옵션 A 측정 신뢰도 단독.

---

## 2. 옵션 D 분기 트리 (인계서 §4-D)

D 결과를 보고 다음 중 하나로 분기:

- **L5 회복 + L1 유지** (목표 종료기준 충족) → 옵션 B 단독 진입
- **L5 여전히 낮음** → 옵션 B + G 병행 (어느 source가 약점인지 본 뒤 contextual prefix 우선순위 결정)
- **L1마저 후퇴** → 액션 1+2 롤백 검토 + RAGAS Claude Haiku 환원 우선

후속 plan 작성 시 `superpowers:writing-plans` 재호출.

---

## 3. 종료 기준 (인계서 §5, 데이터·코드·수치)

### 데이터 산출물 (`~/Downloads/`)
- `notebooklm_post_phase1_<date>_*.xlsx` (옵션 D, 6장)
- `contextual_retrieval_ab_<date>.xlsx` (옵션 B 후속)
- `per_source_ablation_<date>.xlsx` (옵션 G 후속)
- `chunking_poc_<date>.xlsx` (옵션 F 후속)
- `ragas_with_citation_<date>.xlsx` (옵션 H 후속)

### 코드 / 문서
- `backend/scripts/analyze_notebooklm_categories.py` (옵션 D, 본 세션 신규)
- `backend/scripts/build_contextual_prefix.py` (옵션 B 후속)
- `backend/scripts/eval_per_source.py` (옵션 G 후속)
- `backend/src/eval/citation_metrics.py` (옵션 H 후속)
- `backend/src/pipeline/ingestor.py` prefix 옵션 (옵션 B 후속)
- Qdrant 새 컬렉션 `malssum_poc_v2` + 운영 봇 토글 (옵션 B 후속)
- `pytest -q` 통과 (현재 511 passed 유지)

### 수치 종료 목표
- RAGAS Faithfulness: 0.546 → **0.65+**
- RAGAS Context Recall: 0.425 → **0.55+**
- Citation Coverage 첫 측정 + 베이스라인 기록
- Unsupported Claim Rate ≤ **0.15** 목표
- NotebookLM L5 카테고리 hit율 회복 (목표 12%+)

---

## 4. 본 세션(옵션 D) 작업 매트릭스

| Task | 산출 | 파일 |
|---|---|---|
| 1 | 본 dev-log + 브랜치 분기 | `docs/dev-log/2026-04-28-phase-2-plan.md` |
| 2 | 신규 분석 스크립트 실패 테스트 | `backend/scripts/tests/test_analyze_notebooklm_categories.py` |
| 3 | 신규 분석 스크립트 구현 + 회귀 슈트 통과 | `backend/scripts/analyze_notebooklm_categories.py` |
| 4 | semantic_cache 비우기 | (런타임 호출) |
| 5 | NotebookLM 200건 측정 | `~/Downloads/notebooklm_post_phase1_<date>_*.xlsx` × 3 |
| 6 | 좌우 머지 비교 xlsx | 동상 × 3 (`compare_*`) |
| 7 | 카테고리 hit율 분석 + 보고서 | `~/Downloads/notebooklm_post_phase1_<date>_category_analysis.xlsx` + `docs/dev-log/2026-04-28-phase-1-eval-report.md` |
| 8 | 사용자 보고 + 분기 게이트 | (대화) |

세부 단계별 명령은 plan 파일을 참조: `/Users/woosung/.claude/plans/downloads-truewords-phase-2-handoff-md-refactored-hickey.md`.

---

## 5. 운영 메모

- 본 세션은 main에 직접 커밋·푸쉬하지 않음. PR은 옵션 D 결과 보고 후 사용자 승인을 받고 별도로 push + `gh pr create`.
- 환경 변수 `INTENT_CLASSIFIER_FORCE_OFF` 미설정 (액션 1+2+3 ON 그대로 측정).
- `chatbot_id="all"` 기본 사용 — weighted_sources `[B, L, M, N, O]`, weight=1.0, score_threshold=0.1.
- `eval_notebooklm_qa.py`의 `--rate-per-sec=0.3` 기본값 유지.
