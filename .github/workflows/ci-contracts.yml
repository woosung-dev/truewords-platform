name: Contract validation
on:
  workflow_call:
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v7
        with:
          node-version: "22"
          cache: pnpm
      - uses: astral-sh/setup-uv@v4
        with:
          version: "0.10.4"
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - run: pnpm install --frozen-lockfile
      - run: uv sync --frozen --project apps/api
      - name: Regeneration drift and base-branch compatibility
        env:
          # PR: base 브랜치 tip. main push: 직전 커밋. 수동 실행: 비어 있어 스크립트가 merge-base 로 폴백.
          CONTRACT_BASE_REF: ${{ github.event.pull_request.base.sha || github.event.before }}
        run: pnpm contracts:check
      - run: pnpm --filter @truewords/api-client-ts test
      - run: pnpm typecheck
